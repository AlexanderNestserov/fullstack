module.exports.getAllOrders = (req, res)=> {
    res.status(200).json({
        login: 'from',
    });
}

module.exports.create = (req, res)=> {
    res.status(200).json({
        login: 'from',
    });
}
